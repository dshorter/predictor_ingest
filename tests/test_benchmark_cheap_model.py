"""Benchmark cheap model tests — REMOVED (ADR-008).

The two-tier cheap+escalate extraction architecture was replaced by the
Anthropic Batch API (ADR-008). benchmark_cheap_model.py and the escalation
path it tested no longer exist in the pipeline.
"""
# No tests — placeholder to document removal.
